# ============================================================================
# Evidence Engine — variables de entorno
# Copia este archivo a `.env.local` y completa los valores.
# `.env.local` está en .gitignore: los secretos NUNCA se commitean.
# ============================================================================

# ---------------------------------------------------------------------------
# 1) SESIÓN / AUTENTICACIÓN
# ---------------------------------------------------------------------------
# Secreto para firmar el cookie de sesión (JWT). Genera uno con:
#   openssl rand -base64 32
AUTH_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"

# Usuarios del equipo (administrados manualmente — NO hay registro).
# OPCIONAL: el equipo ya viene definido en el código (src/lib/users.ts) con
# contraseñas hasheadas. Define esta variable SOLO si quieres reemplazar esa
# lista sin tocar el código.
# Formato:  email:NombreVisible:contraseña   separados por coma.
# La contraseña puede ir en texto plano o como hash bcrypt con prefijo "bcrypt$".
# Ejemplo:
#   PORTAL_USERS="diana.martin@simonmovilidad.com:Diana:MiClave,taniag@titamedia.com:Tania:OtraClave"
# PORTAL_USERS=

# ---------------------------------------------------------------------------
# 2) GOOGLE (Drive + Sheets) vía APPS SCRIPT
# ---------------------------------------------------------------------------
# Si NO defines estas variables, el portal funciona en MODO LOCAL:
# los archivos se guardan en ./.data y el historial en un JSON local
# (útil para desarrollo; NO persiste en Vercel).
#
# Para persistir en Google (sin cuenta de servicio):
#  1. Abre la hoja → Extensiones → Apps Script.
#  2. Pega el contenido de google-apps-script/Code.gs y cambia TOKEN.
#  3. Implementar → Nueva implementación → Aplicación web
#       · Ejecutar como: Yo   · Quién tiene acceso: Cualquier usuario
#  4. Copia la URL /exec.
#
# URL /exec de la Aplicación web publicada desde la hoja:
APPS_SCRIPT_URL=

# El MISMO token que pusiste en la variable TOKEN dentro de Code.gs:
APPS_SCRIPT_TOKEN=
